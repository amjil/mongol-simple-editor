(ns simple-editor.options
  (:require 
   ["package:flutter/material.dart" :as m]))

(def font-name "MongolianBaiZheng")

(def text-line-options 
  [:line 
   :embedBuilder 
   :styles 
   :read-only
   :controller
   :on-launch-url
   :link-action-picker
   :text-direction
   :custom-style-builder])

